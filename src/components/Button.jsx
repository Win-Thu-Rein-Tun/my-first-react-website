import React from 'react'

const Button = ({styles}) => (
    <button className={`py-4 px-6 bg-blue-gradient ${styles} text-primary outline-none font-poppins font-medium text-[18px] rounded-[10px]`}>
      Get Started
    </button>
)


export default Button